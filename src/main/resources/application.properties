# DATABASE
#spring.datasource.url=jdbc:postgresql://localhost:5432/issueoverflow
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=30MB

# AWS S3 Configuration
cloud.aws.credentials.access-key=${AWS_ACCESS_KEY}
cloud.aws.credentials.secret-key=${AWS_SECRET_KEY}
cloud.aws.region.static=${AWS_REGION}
spring.application.name=${AWS_BUCKET}
cloud.aws.stack.auto=false
# ENABLE THESE TWO LINES BEFORE RUNNING THIS APPLICATION LOCALLY
#spring.application.name=${LOCAL_AWS_BUCKET}
#logging.level.com.amazonaws.util.EC2MetadataUtils=error

# MAIL
spring.mail.host=smtppro.zoho.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true